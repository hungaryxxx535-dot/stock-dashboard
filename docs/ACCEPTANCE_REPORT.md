# Hermes 量化引擎 V1 验收记录

验收日期：2026-08-03（Asia/Shanghai）

## 安全边界

- 环境固定为 `paper`，HTTP 服务只允许监听 `127.0.0.1`/loopback。
- API 使用本地 Bearer Token；真实值仅存在于 Git 忽略的本地 `.env.local`，不写入代码、日志、测试或文档。
- 不包含真实券商连接、真实订单提交、自动实盘交易或自动 Champion 晋级。
- 所有 Hermes 定时任务保持关闭；飞书权限和 09:25 集合竞价数据未通过前不得启用。

## 实际执行结果

- `npm run quant:doctor`：通过。
- `npm run quant:init`：通过；迁移 004、005、006 已执行且可重复运行。
- `npm run quant:test`：44/44 通过，0 失败。
- `npm run typecheck`：通过。
- `npm run lint`：通过；仅有 Next.js lint 命令迁移提示。
- `npm run test`：5 个 Vitest 文件、44 项测试通过。
- `npm run build`：通过；29 个 Next.js 路由构建成功。
- `npm run test:e2e`：45/45 Playwright 测试通过。
- Hermes `QuantClient` 专属测试：5/5 通过。
- 真实 loopback 端到端：健康检查成功；模拟 BUY 限价单 `ACCEPTED -> CANCELLED`；取消后 `frozen_cash=0`；环境为 `paper`。

## 真实 A 股数据链路

数据交易日：2026-08-03；本次同步未使用缓存、未使用测试夹具。

- 证券主数据：AkShare，5,203 只。
- 日线：AkShare，指定 5 股股票池共 5 条，5/5 成功，失败 0，重试 0。
- 5 分钟线：AkShare/Sina fallback，共 240 条，失败 0，重试 0。
- 行业历史：CNINFO，共 52 条。
- 公告：CNINFO，2026-07-01 至 2026-08-03 共 39 条。
- 数据库当前累计：日线 4,342、分钟线 240、行业 52、公告 39。
- 全市场覆盖率：日线与分钟线均为 5/5,203，即 0.0961%；因此数据质量状态为 `degraded`，不得表述为全市场同步成功。
- 09:25 集合竞价委托簿：未接入。

## 最小真实基准回测

最终 run id：`a3474ddb-a35a-4865-8ba8-71a8f799b7b1`

- 股票池：`600036`、`600519`、`000333`、`300750`、`601318`。
- 模型：`momentum-60d-rebalance-20d-v1`。
- 训练：2023-01-03 至 2024-10-21；验证：2024-10-22 至 2025-07-07；测试：2025-07-08 至 2026-08-03。
- 使用下一根可交易日线成交，计入佣金、最低佣金、印花税和滑点，并处理 T+1、停牌、涨跌停和不可成交。
- 交易 6 笔；胜率 50%；盈亏比 1.347948；Profit Factor 1.347948；单笔期望 1,787.65 元。
- 总收益 1.0726%；最大回撤 -3.6011%；Sharpe 0.24824；最大连续亏损 2。
- 平均 MFE 15.5847%；平均 MAE -5.4216%；不可成交率 0%；滑点 755.22 元；费用 897.91 元。
- 同期 5 股等权 5.9914%；固定随机种子基准 -0.4408%；沪深 300 价格基准 13.6234%（指数基准未计交易成本）。

可信度结论：该结果不能证明策略有效。股票池只有 5 只且仅 6 笔交易，使用日线近似成交，也没有历史时点指数成分库；仅可证明真实数据、回测撮合、费用和产物保存链路可执行。

## Paper Broker 覆盖

已通过订单状态机、T+1、涨跌停、停牌、部分成交、资金不足拒单、滑点与手续费、最小佣金、数据时间戳缺失/陈旧拒绝、重复提交幂等、订单到期释放资金、五账户隔离等测试。

## 飞书与定时任务

- xiaobu 身份的飞书 WebSocket 网关可连接。
- 测试消息发送被飞书返回错误 `99991672`，原因是应用缺少 `im:message:send`、`im:message` 或 `im:message:send_as_bot` 权限；消息 ID 不存在。
- 因飞书真实发送失败且集合竞价数据缺失，07:30、08:00、09:25、15:10、15:30 五个前向模拟任务全部保持关闭。

## 运维命令

- 初始化：`npm run quant:init`
- 诊断：`npm run quant:doctor`
- 启动 API：`npm run quant:api:start`
- 查看 API：`npm run quant:api:status`
- 停止 API：`npm run quant:api:stop`
- 量化测试：`npm run quant:test`
- 最小回测：`npm run quant:backtest`

20 个真实交易日后才能进行首次阶段评审；60 个真实交易日后才能进行完整模型评审，不得缩短或伪造观察周期。
