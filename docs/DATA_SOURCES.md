# 数据源与完整性

## AkShare

适配器：`hermes_quant/data/akshare_provider.py`。

- 当前上市证券：上交所主板、科创板和深交所A股列表，包含真实上市日期。
- 日线：`stock_zh_a_hist`，不复权原始OHLCV，记录抓取时间、数据时间和内容哈希版本。
- 失败策略：超时、指数退避重试、限速、本地缓存恢复；缓存恢复必须标记 `stale=true` 和原始错误。
- 代理：只从 `HERMES_HTTP_PROXY` 配置读取，禁止业务代码硬编码。AkShare内部请求是否完全遵循代理取决于对应上游接口，部署前需单独健康检查。

2026-08-03实际联网验证：

- 当前证券主表：5203条，run_id `04c17eda-bba2-4dd6-b3ff-df92d4ef6ff2`，版本 `f9ca33e076dc551d`。
- `600036` 日线：2024-01-02至2024-01-10共7条，run_id `902b572f-d6d9-4fca-9b50-ed1242c8e9bf`，版本 `2fd6e20037c2e3c6`，质量错误0。
- 对同范围执行增量同步返回0条和 `already_up_to_date`。

## 尚缺数据

当前未获得足以做全市场可信历史回测的完整数据：已退市证券全集、历史ST/*ST、停复牌、历史涨跌停规则来源、代码变更、复权因子、公司行动、公告精确发布时间、财务实际披露时间、历史行业和指数成分、完整交易日历、分钟行情。数据库已建表，缺失项不得用当前快照或假数据补齐。

真实集成与测试夹具严格分开；`TEST_FIXTURE` 只能验证代码机制。
