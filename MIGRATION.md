# V1 到 V2 迁移

V2 会检测旧版浏览器键值，在设置页提示“检测到 V1 数据”。用户触发迁移时先创建“旧版数据迁移前自动备份”，再转换为 Schema v2 并写入 IndexedDB。迁移成功后页面只读取新 Repository。

为了防止公开泄露，旧源码中的真实静态数据没有复制进新 Demo。任务执行时的原文件已在本机忽略目录保存，未提交到 Git。迁移不会猜测或修改数量、成本、现金与交易结论；字段无法确认时保留警告并要求人工复核。

备份恢复：设置 → 导出 JSON；在新浏览器进入导入中心或设置 → 选择 JSON → Zod 校验 → 自动创建导入前快照 → 恢复。失败不会改变现有状态。
