# Hermes Agent 工程约束

## 项目目标

本项目用于 A 股研究、点时选股、历史回测和模拟交易，并提供可选的飞书通知。系统只允许研究、回测与 Paper Trading；任何真实证券账户连接、真实委托或自动下单都禁止实现和启用。

## 目录结构

- `src/`：Next.js 展示、研究与本地数据工作流。
- `akshare-service/`：现有轻量行情与宏观数据服务。
- `hermes_quant/`：可审计的 Python 数据、点时股票池、回测、模拟券商、调度和消息核心。
- `migrations/`：只允许向前演进的 SQLite 迁移。
- `tests_quant/`：量化核心的离线确定性测试和显式联网集成测试。
- `docs/`：架构、数据、方法、运维和验收记录。
- `tools/`：本地启动、安全扫描与烟雾测试脚本。

## 启动与测试

- 前端开发：`npm run dev`
- 前端与 AkShare：`npm run services`
- 量化数据库：`python -m hermes_quant.cli init-db`
- 量化离线烟雾：`python -m hermes_quant.cli smoke`
- 前端测试：`npm run security:scan && npm run typecheck && npm run lint && npm run test`
- 量化测试：`python -m unittest discover -s tests_quant -v`
- 端到端：先执行 `npm run build`，再执行 `npm run smoke` 与 `npm run test:e2e`

## 数据与安全边界

- 密码、Token、App Secret、Cookie、API Key 只能通过环境变量注入，禁止进入代码、日志、报告和 Git。
- 真实持仓、成交、备份和截图必须保存在被忽略的本地目录，禁止使用 `public/` 作为私有数据仓库。
- 数据失败必须留下来源、请求时间、数据时间、异常类型和失败状态；禁止用测试夹具或静态数据伪装真实成功。
- 代理、超时、重试、限速和缓存必须由统一配置或 Provider 层管理，业务代码不得硬编码代理。

## 禁止真实交易

- 不得导入真实券商交易 SDK，不得建立真实券商会话，不得发送真实订单。
- `PaperBroker` 是唯一允许的成交接口，输出必须带 `PAPER` 标识。
- 如未来提出真实账户连接，必须停止执行并取得人工授权；当前仓库仍不得实现该能力。

## 防止未来数据与理想化成交

- 股票池和特征只可读取 `effective_from <= as_of < effective_to` 且 `published_at <= signal_time` 的记录。
- 财务数据使用实际披露时间，不得用报告期末替代披露日。
- 收盘后形成的信号不得按同日收盘价成交；默认最早在下一根可交易 K 线撮合。
- 回测必须保留退市证券、未成交信号、失败订单和全部费用，不得筛掉亏损交易。
- 测试集只能作一次性样本外评估，不得反复调参。

## Git 提交规范

- 接管前先建立安全基线提交；重要阶段单独提交，提交信息说明可验证成果。
- 不覆盖原始历史，不使用 `reset --hard`，不提交生成缓存或私人数据。
- 提交前运行安全扫描和与变更相称的测试；最终工作区必须干净。

## 数据库迁移规范

- 迁移文件按递增编号命名，一经提交不得修改既有迁移语义。
- 迁移必须幂等记录版本，并在空库和已迁移库上测试。
- 历史状态表必须包含有效起止时间；时间戳统一存储为带时区 ISO-8601 或明确的交易日期。
- 禁止以清库重建作为常规升级方式；破坏性迁移必须先备份并人工批准。

## 回测与模拟成交规范

- 固定随机种子、数据版本、策略版本和参数版本，保证可复现。
- 纳入 T+1、停牌、涨跌停、一字板、最小交易单位、资金冻结、成交量参与率、部分成交、手续费、印花税、滑点与冲击成本。
- 分钟数据缺失时只能声明日线近似精度，不得伪装盘中精确撮合。
- 每个策略独立报告；Champion 必须与等权、随机和风格基准使用同口径比较。

## 定时任务上线门槛

- 所有任务默认关闭，必须幂等、可补跑、可重试并记录 `run_id`。
- 08:00 与 09:25 自动推送在验收报告明确批准前必须保持关闭。
- 飞书消息必须去重、记录失败、拆分长消息，并显示：`仅为模拟交易系统输出，不代表真实证券交易指令。`
- Champion/Challenger 的晋级仍需至少 20 个完整模拟交易日；60 个交易日才可形成更可靠评估，任何自动化都不得压缩或伪造该时间。
