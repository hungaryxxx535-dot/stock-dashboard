# 安全与隐私

本仓库曾包含个人持仓硬编码，因此强烈建议将 GitHub 仓库改为私有。按任务要求不改写 Git 历史；V2 只清理当前分支和后续架构。

真实持仓、成本、现金、盈亏、账户截图和交易记录仅保存在浏览器 IndexedDB 或用户自行保管的备份中。不要把备份、截图、`.env.local`、日志或测试产物提交到 Git。`.gitignore` 已忽略本地私有目录和备份。

提交前运行 `npm run security:scan`。它阻止已知私有数据文件、外项目文件和高置信度密钥进入当前追踪集合。真实凭证只能放入本地/Vercel 环境变量。错误日志不得打印请求凭证、完整资产或账户数据。

发现泄露时立即撤销密钥、把仓库改私有并联系 GitHub 处理历史；不要仅删除最新提交后假定历史已清除。
